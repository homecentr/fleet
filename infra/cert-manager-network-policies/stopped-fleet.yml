defaultNamespace: cert-manager

dependsOn:
  - name: homecentr-infra-cert-manager

targetCustomizations:
  - name: lab
    clusterSelector:
      matchLabels:
        env: lab
    yaml:
      overlays:
      - lab

  - name: prod
    clusterSelector:
      matchLabels:
        env: prod
    yaml:
      overlays:
      - prod
