defaultNamespace: kube-system

helm:
  chart: "" # local chart
  repo: ""
  version: ""
  releaseName: local-storage
  values:
    imageVersion: v0.0.23
    volumes:
      - name: services
        path: "/mnt/gfs/k3s-services"
        bindingMode: Immediate
        reclaimPolicy: delete