defaultNamespace: kube-system

name: local-storage-provisioner

labels:
  namespace: kube-system

helm:
  force: true # to allow replacing immutable resources (e.g. deployment labels)
  chart: "./chart"
  releaseName: local-storage-provisioner
  valuesFiles:
    - values.yml
